import { test, expect } from "../setup";

test.describe("test request pickup suite", () => {
    test("test Pickup Creation, Full Payload & Quote Conversion", async ({
        api,
        apiBaseUrl,
    }) => {});

    test("test negative submissions (past dates, invalid types & partial data)", async ({
        api,
        apiBaseUrl,
    }) => {});

    test("test unconfirmed pickup request limit", async ({
        api,
        apiBaseUrl,
    }) => {});

    test("test same-day service restrictions cutoff & cross-Country", async ({
        api,
        apiBaseUrl,
    }) => {});
});
